mixin option(categories, prefix = '', parent = '')
    each item in categories
        option(value=item.id selected=(item.id === parent ? true : false))= prefix + item.name
        if item.children && item.children.length
            +option(item.children, prefix + '-- ', parent)
